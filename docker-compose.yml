services:
  pwnybot:
    build: ./server
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "127.0.0.1:8787:8787"
    volumes:
      - pwnybot_data:/app/data

volumes:
  pwnybot_data:
